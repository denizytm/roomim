{
  "name": "hoomies",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @hoomies/web",
    "build": "npm run build --workspace @hoomies/web",
    "lint": "npm run lint --workspace @hoomies/web",
    "typecheck": "npm run typecheck --workspace @hoomies/web"
  }
}
